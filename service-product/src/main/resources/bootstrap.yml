spring:
  application:
    name: service-product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: infra-config-server

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/